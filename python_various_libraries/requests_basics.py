# import requests
# import json

# print(requests.post(
#     url="api_url",
#     headers={"X-Requested-With": "curl", "Content-Type": "text/xml"},
#     data="xml_data",
#     auth=("username", "password"),
# ))

# print(requests.post(url="api_url", data=json.dumps({"test": "data"}, headers={"Content-Type": "application/json"})))
